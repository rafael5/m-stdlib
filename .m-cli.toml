# m-cli configuration — modern M project.
# See ~/claude/templates/m-project/.m-cli.toml for documentation.

[lint]
rules = "pythonic"
target-engine = "any"

[fmt]
rules = "pythonic-lower"

[lsp]
"hover.format" = "markdown"
